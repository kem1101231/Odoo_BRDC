<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="brdc_collection.list">
      <field name="name">brdc_collection list</field>
      <field name="model">brdc_collection.brdc_collection</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="brdc_collection.action_window">
      <field name="name">brdc_collection window</field>
      <field name="res_model">brdc_collection.brdc_collection</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="brdc_collection.action_server">
      <field name="name">brdc_collection server</field>
      <field name="model_id" ref="model_brdc_collection_brdc_collection"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="brdc_collection" id="brdc_collection.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="brdc_collection.menu_1" parent="brdc_collection.menu_root"/>
    <menuitem name="Menu 2" id="brdc_collection.menu_2" parent="brdc_collection.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="brdc_collection.menu_1_list" parent="brdc_collection.menu_1"
              action="brdc_collection.action_window"/>
    <menuitem name="Server to list" id="brdc_collection" parent="brdc_collection.menu_2"
              action="brdc_collection.action_server"/>
    -->
  </data>
</odoo>